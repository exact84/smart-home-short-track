import { SensorValuePipe } from './sensor-value-pipe';

describe('SensorValuePipePipe', () => {
  it('create an instance', () => {
    const pipe = new SensorValuePipe();
    expect(pipe).toBeTruthy();
  });
});
